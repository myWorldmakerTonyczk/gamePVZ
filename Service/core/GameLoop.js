//游戏主循环（生命周期）
function GameLoop() {
     switch(currentState) {
        case GameState.START:
            break;
        case GameState.PLAYING:
            break;
        case GameState.PAUSED:
            break;
        case GameState.WIN:
            break;
        case GameState.LOSE:
            break;
     }
}